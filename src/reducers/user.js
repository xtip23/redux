const userReducer = (state, action) =>{
    switch (action.type) {
        case 'ADD_USER':
            
            break;
        case 'DELETE_USER':
            
            break;
    
        default:
            return state;
    }
}

export default userReducer